#include <stdio.h>
 
int main()
{
int a=0,b=0,sum=0;    		
printf("Enter two numbers with min being 1 and max 5..."); 
scanf("%d%d",&a,&b);      
sum=(a+b)/2;                   
printf("The average is %d",sum); 
return 0;
}
